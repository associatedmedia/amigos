# Agent Directives: AmigosDriver

**Framework**: React Native

## Instructions
1. **Local MCP Priority**: First and foremost, when utilizing models, executing tasks, or querying data, actively default to using the local Ollama MCP server. Only fallback to Cloud Gemini models/tools if the local MCP server fails or cannot fulfill the request.
2. **Context**: Ensure that any new logic or screens built for this Driver utility app follow established React Native mobile patterns.
